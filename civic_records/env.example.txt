# Copy this to .env and adjust as needed for local development.
# Do NOT commit .env — it's in .gitignore.

DEBUG=1
SECRET_KEY=dev-only-change-me-in-production-but-fine-for-localhost

POSTGRES_DB=civic_records
POSTGRES_USER=civic
POSTGRES_PASSWORD=civic_dev_password

ALLOWED_HOSTS=localhost,127.0.0.1

# ─── LLM provider (optional) ──────────────────────────────────────────
# All LLM features (enrich_records, discover_source_taxonomy,
# relink_projects --llm) are opt-in. The site renders fully without any
# of this configured. Default provider is cloudflare so the project
# deploys to Cloudflare without code changes.

LLM_PROVIDER=cloudflare

# Cloudflare Workers AI (default — free tier covers ~10k neurons/day)
# Get a token at https://dash.cloudflare.com/profile/api-tokens
# (scope: Workers AI - Read).
# CLOUDFLARE_ACCOUNT_ID=
# CLOUDFLARE_API_TOKEN=
# CLOUDFLARE_MODEL=@cf/meta/llama-3.1-8b-instruct

# Anthropic Claude (alternative — set LLM_PROVIDER=anthropic)
# ANTHROPIC_API_KEY=

# ─── Ingest source URLs (optional) ────────────────────────────────────
# EPC_PARCELS_FEATURESERVER=https://gisservices.elpasoco.com/arcgis2/rest/services/HubPublic/Parcels/MapServer/0
# SOCRATA_DATASET_URL=https://policedata.coloradosprings.gov/resource/{id}.json
# SOCRATA_APP_TOKEN=
