{
  "name": "cheetochopsticks-worker",
  "private": true,
  "version": "0.0.0",
  "description": "Cloudflare Worker + static assets for cheetochopsticks.com",
  "scripts": {
    "catalog": "node --experimental-strip-types worker/generate-catalog.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250628.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
